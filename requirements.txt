# Stable, minimal stack for running MOSS-Audio locally.
# - NO torchaudio, NO torchcodec, NO system FFmpeg ABI.
# - librosa + soundfile handle all audio decoding.
# - numpy<2 to avoid ABI breaks against compiled wheels (torch, soundfile,
#   sentencepiece) that haven't all migrated yet.

torch>=2.4,<2.10
# Pin transformers to the version upstream MOSS-Audio tests against
# (per their pyproject.toml). Newer Whisper API in 4.58+ has GQA-shaped
# Q/K/V projections that MOSS-Audio's vendored audio encoder doesn't
# handle, producing 'tensor a (50) must match tensor b (20)' at runtime.
transformers==4.57.1
accelerate>=0.30
safetensors>=0.4

huggingface_hub[cli]>=0.25
sentencepiece>=0.2
protobuf>=4

librosa>=0.10
soundfile>=0.12
numpy>=1.26,<2
